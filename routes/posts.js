/* 
    Ruta: /api/v1/posts
*/
const { Router } = require('express');

//Inicializamos router
const router = Router();

//Rutas
router.get('/');
router.post('/');
router.delete('/:id',);
router.put('/:id');

module.exports = router;